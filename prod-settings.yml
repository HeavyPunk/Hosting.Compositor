app:
  port: 8080
  configuration: release

hypervisor:
  services:
    scripts-dir: ./
    ports-service:
      db-path: ./ports-storage.db
    outbound-ip: 127.0.0.1
    container-create-attempts: 5
